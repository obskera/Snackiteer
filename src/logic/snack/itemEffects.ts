/** Item effects removed — items are now category + quality only. */

export const effectPriceBonus = (_effectId: string | undefined): number => 0;
export const effectSellBonus = (_effectId: string | undefined): number => 0;
export const effectDamageReduction = (_effectId: string | undefined): number =>
    0;
export const effectKickChanceMult = (_effectId: string | undefined): number =>
    1;
